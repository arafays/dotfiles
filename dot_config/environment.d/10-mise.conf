# Mise environment configuration
# This file is sourced by systemd user session manager
# Applications launched from desktop environment will inherit these variables

# Include mise shims directory in PATH
PATH="${HOME}/.local/share/mise/shims:${PATH}"
