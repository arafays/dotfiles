# ~/.profile — POSIX login shell environment
# Sourced by sh, bash, zsh (via .zshrc), and any login shell.
# Fish sources environment.d separately via fenv.

# System-wide profile
[ -f /etc/profile ] && . /etc/profile

# GPG TTY (not in environment.d — needs runtime tty)
command -v tty >/dev/null 2>&1 && export GPG_TTY="$(tty 2>/dev/null)"

# PATH — idempotent additions
_prepend() {
  case ":$PATH:" in *:"$1":*) ;; *) PATH="$1:$PATH" ;; esac
}
_append() {
  case ":$PATH:" in *:"$1":*) ;; *) PATH="$PATH:$1" ;; esac
}

_prepend "$HOME/.local/bin"

# Android SDK tool dirs (append, only if they exist)
if [ -d "$HOME/Android/Sdk" ]; then
  for _dir in \
    "$HOME/Android/Sdk/emulator" \
    "$HOME/Android/Sdk/cmdline-tools/latest/bin" \
    "$HOME/Android/Sdk/platform-tools"; do
    [ -d "$_dir" ] && _append "$_dir"
  done
  unset _dir
fi

# Deduplicate PATH while preserving order (environment.d may append duplicates
# when .profile is sourced more than once, e.g. via BASH_ENV).
_path_dedup() {
  _pd_new_path=""
  _pd_old_ifs="$IFS"
  IFS=':'
  for _pd_item in $PATH; do
    case ":$_pd_new_path:" in
    *:"$_pd_item":*) ;;
    *) [ -n "$_pd_item" ] && _pd_new_path="${_pd_new_path:+$_pd_new_path:}$_pd_item" ;;
    esac
  done
  IFS="$_pd_old_ifs"
  PATH="$_pd_new_path"
  unset _pd_new_path _pd_item _pd_old_ifs
}
_path_dedup
unset _path_dedup

export PATH
unset _prepend _append
