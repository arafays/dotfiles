# Shell runtime configuration for non-interactive shells.
# Makes non-interactive bash source ~/.profile so it inherits the same
# environment (XDG, editor, PATH, Android, mise, secrets, etc.).
BASH_ENV="$HOME/.profile"
